/** Injected by vite.config.ts at build time — see the comment there. */
declare const __BUILD_NUMBER__: string

/** Injected by vite.config.ts at build time — docs/todo.md entry 93. A
 *  JSON-encoded string; queue-panel.ts is the only reader. */
declare const __QUEUE__: string
